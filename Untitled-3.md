Consider gitignore.go and the full test suite.

What is the risk that gitignore.go is biased against "just" solving the test suite, and not actually fulfilling gitignore parity?

If you believe it is biased, return a concentrated set of YAML defined tests (20-30), which aim to prove that above is the case or not.
